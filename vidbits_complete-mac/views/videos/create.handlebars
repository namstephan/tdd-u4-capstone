<!DOCTYPE html>
<html>
  <head>
    <title>Save a video</title>
    <link rel="icon" type="/image/ico" href="/img/favicon.ico">
    <link href="https://fonts.googleapis.com/css?family=Montserrat:300,500" rel="stylesheet">
    <link rel='stylesheet' href='stylesheets/style.css'>
  </head>
  <body>
    <div class="title-container">
      <div class="title-logo">
        <img src="img/logo.svg" alt="">
      </div>
    </div>
    <div id="Save-Video-Container" class="contents-container">
      <h1 id="create-video-h1">Save a video</h1>
      <form class="input-form" action="/videos/create" method="post">
      <label class="form-label" for="title">Title</label>
      {{#if error.title}}<span class="title-error">{{error.title.message}}</span>{{/if}}
      <input id="title-input" name="title" maxlength="50" value="{{#if video.title}}{{video.title}}{{/if}}"/>
      <label class="form-label" for="description">Description</label>
      {{#if error.description}}<span class="description-error">{{error.description.message}}</span>{{/if}}
      <textarea id="description-input" name="description" maxlength="50">{{#if video.description}}{{video.description}}{{/if}}</textarea>
      <label class="form-label" for="url">url</label>
      {{#if error.url}}<span class="url-error">{{error.url.message}}</span>{{/if}}
      <input id="url-input" name="url" maxlength="200" value="{{#if video.url}}{{video.url}}{{/if}}"/>
      <input id="submit-button" name="submit-button" type="submit" value="Submit"/>
  </div>
</body>
</html>
